module.exports = {
    presets: [
      'next/babel', // Use Next.js default Babel preset (includes react and other optimizations)
      '@babel/preset-react' // Explicitly add the React preset to handle JSX
    ],
    plugins: [
      // You can add additional plugins here if needed
    ],
  };
  